import React from "react";

function Header(){
    return(
        <header>
            <h1 className="font-color">
                Google Keep
            </h1>
        </header>
    );
}

export default Header;